<Window x:Class="LanguageSchoolERP.App.Windows.AddContractWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Contract" Height="560" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Create Contract" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Enrollment (required)"/>
            <ComboBox Margin="0,4,0,12"
                      ItemsSource="{Binding EnrollmentOptions}"
                      SelectedItem="{Binding SelectedEnrollment}"/>

            <TextBlock Text="Created At" VerticalAlignment="Center"/>
            <DatePicker x:Name="CreatedAtDatePicker" Margin="0,4,0,12" Height="34" VerticalContentAlignment="Center" SelectedDate="{Binding CreatedAt}" PreviewMouseLeftButtonDown="CreatedAtDatePicker_OnPreviewMouseLeftButtonDown">
                <DatePicker.Resources>
                    <Style TargetType="{x:Type DatePickerTextBox}">
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="10,6"/>
                    </Style>
                </DatePicker.Resources>
            </DatePicker>

            <TextBlock Text="Student Name"/>
            <TextBox Margin="0,4,0,12" Height="34" Text="{Binding StudentName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Guardian Name"/>
            <TextBox Margin="0,4,0,12" Height="34" Text="{Binding GuardianName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Notes"/>
            <TextBox Margin="0,4,0,0" Height="100"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <DockPanel Grid.Row="2" Margin="0,14,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="IndianRed" VerticalAlignment="Center"/>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Cancel" Width="100" Margin="0,0,8,0" IsCancel="True"/>
                <Button Content="Save" Width="120" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
