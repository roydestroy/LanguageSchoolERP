<Window x:Class="LanguageSchoolERP.App.Windows.AddContractWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Δημιουργία συμφωνητικού" Height="623" Width="518"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Padding" Value="2,4"/>
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="DatePicker" BasedOn="{StaticResource {x:Type DatePicker}}">
            <Setter Property="Padding" Value="3,4"/>
        </Style>
        <DataTemplate x:Key="EnrollmentOptionTemplate">
            <TextBlock Text="{Binding Label}"/>
        </DataTemplate>
    </Window.Resources>


    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Δημιουργία συμφωνητικού" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Εγγραφή (υποχρεωτικό)"/>
            <ComboBox Margin="0,4,0,12"
                      ItemsSource="{Binding EnrollmentOptions}"
                      ItemTemplate="{StaticResource EnrollmentOptionTemplate}"
                      Foreground="{StaticResource Brush.TextPrimary}"
                      SelectedItem="{Binding SelectedEnrollment}"/>

            <TextBlock Text="Ημ/νία δημιουργίας"/>
            <DatePicker Margin="0,4,0,12" SelectedDate="{Binding CreatedAt}"/>

            <TextBlock Text="Όνομα μαθητή"/>
            <TextBox Margin="0,4,0,12" Text="{Binding StudentName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Όνομα κηδεμόνα"/>
            <TextBox Margin="0,4,0,12" Text="{Binding GuardianName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Σημειώσεις"/>
            <TextBox Margin="0,4,0,0" Height="100"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding ErrorMessage}" Foreground="IndianRed" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Ακύρωση" Width="100" Margin="0,0,8,0" IsCancel="True"/>
                <Button Content="Αποθήκευση" Width="120" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
