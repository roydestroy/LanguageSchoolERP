<Window x:Class="LanguageSchoolERP.App.Windows.AddContractWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Contract" Height="640" Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Create Contract" Style="{StaticResource PageTitleText}"/>
            <TextBlock Text="Complete the details below to create a new student contract."
                       Margin="0,8,0,0"
                       Style="{StaticResource CaptionText}"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,24,0,16" Style="{StaticResource CardBorder}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Contract Details" Style="{StaticResource SectionTitleText}" Margin="0,0,0,8"/>

                    <TextBlock Text="Template (required)" Style="{StaticResource CaptionText}"/>
                    <ComboBox ItemsSource="{Binding TemplateOptions}"
                              SelectedItem="{Binding SelectedTemplate}"/>

                    <TextBlock Text="Enrollment (required)" Style="{StaticResource CaptionText}"/>
                    <ComboBox ItemsSource="{Binding EnrollmentOptions}"
                              SelectedItem="{Binding SelectedEnrollment}"/>

                    <TextBlock Text="Created At" Style="{StaticResource CaptionText}"/>
                    <DatePicker SelectedDate="{Binding CreatedAt}"/>

                    <TextBlock Text="Student Name" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/>
                    <TextBox Text="{Binding StudentName, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Guardian Name" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/>
                    <TextBox Text="{Binding GuardianName, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Notes" Style="{StaticResource CaptionText}" Margin="0,4,0,0"/>
                    <TextBox Height="120"
                             Margin="0,4,0,0"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             TextWrapping="Wrap"
                             Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <DockPanel Grid.Row="2" LastChildFill="False">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource Brush.Danger}" VerticalAlignment="Center"/>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Cancel" Width="120" Margin="0,0,8,0" IsCancel="True" Style="{StaticResource SecondaryButton}"/>
                <Button Content="Save Contract" Width="140" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
