<Window x:Class="LanguageSchoolERP.App.Windows.AddProgramWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Νέο πρόγραμμα"
        Height="190"
        Width="430"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Προσθήκη νέου προγράμματος"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Όνομα προγράμματος"/>
        <TextBox Grid.Row="2"
                 x:Name="ProgramNameTextBox"
                 Margin="0,4,0,0"
                 Text="{Binding ProgramName, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Ακύρωση" Width="100" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Προσθήκη" Width="110" IsDefault="True" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
