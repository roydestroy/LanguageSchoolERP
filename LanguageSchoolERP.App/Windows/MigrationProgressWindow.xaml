<Window x:Class="LanguageSchoolERP.App.Windows.MigrationProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ενημέρωση βάσης"
        Height="170" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Γίνεται ενημέρωση της βάσης για τη νέα έκδοση"
                   FontSize="14"
                   FontWeight="SemiBold"/>

        <ProgressBar Grid.Row="2"
                     Height="18"
                     IsIndeterminate="True"/>

        <TextBlock x:Name="StatusText"
                   Grid.Row="4"
                   Text="Παρακαλώ περιμένετε..."
                   TextWrapping="Wrap"
                   Opacity="0.9"/>
    </Grid>
</Window>
