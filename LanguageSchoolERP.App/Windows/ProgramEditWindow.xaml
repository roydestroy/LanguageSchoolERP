<Window x:Class="LanguageSchoolERP.App.Windows.ProgramEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Height="290"
        Width="430"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Όνομα προγράμματος"/>
        <TextBox Grid.Row="1" Margin="0,4,0,10" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <CheckBox Grid.Row="2" Content="Μεταφορά" IsChecked="{Binding HasTransport}" Margin="0,0,0,6"/>
        <CheckBox Grid.Row="3" Content="Study Lab" IsChecked="{Binding HasStudyLab}" Margin="0,0,0,6"/>
        <CheckBox Grid.Row="4" Content="Βιβλία" IsChecked="{Binding HasBooks}"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Ακύρωση" Width="100" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Αποθήκευση" Width="110" IsDefault="True" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
